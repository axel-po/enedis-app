import React from "react";
import { View } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

const Home = () => {
  return (
    <SafeAreaView>
      {/* <RegisterForm /> */}
      <View className="text-red-600">TEST</View>
    </SafeAreaView>
  );
};

export default Home;
